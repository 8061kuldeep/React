import React, { Component } from "react";
import Form from "./Form";

export class User extends Component {
  render() {
    return (
      <div>
        <Form></Form>
      </div>
    );
  }
}

export default User;
